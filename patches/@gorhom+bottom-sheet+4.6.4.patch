diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx
index 33495b1..aee3e40 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx
@@ -12,13 +12,9 @@ import Animated, {
   Extrapolate,
   useAnimatedStyle,
   useAnimatedReaction,
-  useAnimatedGestureHandler,
   runOnJS,
 } from 'react-native-reanimated';
-import {
-  TapGestureHandler,
-  TapGestureHandlerGestureEvent,
-} from 'react-native-gesture-handler';
+import { Gesture, GestureDetector } from 'react-native-gesture-handler';
 import { useBottomSheet } from '../../hooks';
 import {
   DEFAULT_OPACITY,
@@ -91,16 +87,15 @@ const BottomSheetBackdropComponent = ({
   );
   //#endregion
 
-  //#region tap gesture
-  const gestureHandler =
-    useAnimatedGestureHandler<TapGestureHandlerGestureEvent>(
-      {
-        onFinish: () => {
-          runOnJS(handleOnPress)();
-        },
-      },
-      [handleOnPress]
-    );
+  //#region tap gesture (Reanimated 4 compatible: Gesture.Tap instead of useAnimatedGestureHandler)
+  const tapGesture = React.useMemo(
+    () =>
+      Gesture.Tap().onEnd(() => {
+        'worklet';
+        runOnJS(handleOnPress)();
+      }),
+    [handleOnPress]
+  );
   //#endregion
 
   //#region styles
@@ -162,9 +157,7 @@ const BottomSheetBackdropComponent = ({
   );
 
   return pressBehavior !== 'none' ? (
-    <TapGestureHandler onGestureEvent={gestureHandler}>
-      {AnimatedView}
-    </TapGestureHandler>
+    <GestureDetector gesture={tapGesture}>{AnimatedView}</GestureDetector>
   ) : (
     AnimatedView
   );
diff --git a/node_modules/@gorhom/bottom-sheet/src/hooks/useGestureHandler.ts b/node_modules/@gorhom/bottom-sheet/src/hooks/useGestureHandler.ts
index eb50e72..f0dc00f 100644
--- a/node_modules/@gorhom/bottom-sheet/src/hooks/useGestureHandler.ts
+++ b/node_modules/@gorhom/bottom-sheet/src/hooks/useGestureHandler.ts
@@ -1,20 +1,18 @@
-import Animated, { useAnimatedGestureHandler } from 'react-native-reanimated';
+import Animated, { runOnUI, useSharedValue } from 'react-native-reanimated';
 import {
   State,
   PanGestureHandlerGestureEvent,
 } from 'react-native-gesture-handler';
+import { useCallback } from 'react';
 import { GESTURE_SOURCE } from '../constants';
 import type {
   GestureEventContextType,
   GestureEventHandlerCallbackType,
 } from '../types';
 
-const resetContext = (context: any) => {
+const resetContext = (context: GestureEventContextType) => {
   'worklet';
-
-  Object.keys(context).map(key => {
-    context[key] = undefined;
-  });
+  context.didStart = undefined;
 };
 
 export const useGestureHandler = (
@@ -25,72 +23,53 @@ export const useGestureHandler = (
   handleOnActive: GestureEventHandlerCallbackType,
   handleOnEnd: GestureEventHandlerCallbackType
 ): ((event: PanGestureHandlerGestureEvent) => void) => {
-  const gestureHandler = useAnimatedGestureHandler<
-    PanGestureHandlerGestureEvent,
-    GestureEventContextType
-  >(
-    {
-      onActive: (payload, context) => {
-        if (!context.didStart) {
-          context.didStart = true;
-
-          state.value = State.BEGAN;
-          gestureSource.value = type;
-
-          handleOnStart(type, payload, context);
-          return;
-        }
-
-        if (gestureSource.value !== type) {
-          return;
-        }
+  const contextRef = useSharedValue<GestureEventContextType>({ didStart: false });
 
-        state.value = payload.state;
-        handleOnActive(type, payload, context);
-      },
-      onEnd: (payload, context) => {
-        if (gestureSource.value !== type) {
-          return;
-        }
+  const gestureHandler = useCallback(
+    (event: PanGestureHandlerGestureEvent) => {
+      const payload = event.nativeEvent as Parameters<
+        GestureEventHandlerCallbackType<GestureEventContextType>
+      >[1];
+      const runWorklet = () => {
+        runOnUI((p: typeof payload) => {
+          'worklet';
+          const context = contextRef.value;
+          const payloadState = p.state ?? State.ACTIVE;
 
-        state.value = payload.state;
-        gestureSource.value = GESTURE_SOURCE.UNDETERMINED;
-
-        handleOnEnd(type, payload, context);
-        resetContext(context);
-      },
-      onCancel: (payload, context) => {
-        if (gestureSource.value !== type) {
+        if (payloadState === State.BEGAN || payloadState === State.ACTIVE) {
+          if (!context.didStart) {
+            context.didStart = true;
+            state.value = State.BEGAN;
+            gestureSource.value = type;
+            handleOnStart(type, p, context);
+            if (payloadState === State.ACTIVE) {
+              state.value = payloadState;
+              handleOnActive(type, p, context);
+            }
+            return;
+          }
+          if (gestureSource.value !== type) return;
+          state.value = payloadState;
+          handleOnActive(type, p, context);
           return;
         }
 
-        state.value = payload.state;
+        if (gestureSource.value !== type) return;
+        state.value = payloadState;
         gestureSource.value = GESTURE_SOURCE.UNDETERMINED;
-
+        handleOnEnd(type, p, context);
         resetContext(context);
-      },
-      onFail: (payload, context) => {
-        if (gestureSource.value !== type) {
-          return;
-        }
-
-        state.value = payload.state;
-        gestureSource.value = GESTURE_SOURCE.UNDETERMINED;
-
-        resetContext(context);
-      },
-      onFinish: (payload, context) => {
-        if (gestureSource.value !== type) {
-          return;
-        }
-
-        state.value = payload.state;
-        gestureSource.value = GESTURE_SOURCE.UNDETERMINED;
-
-        resetContext(context);
-      },
+        })(payload);
+      };
+      // Defer so shared value read doesn't happen during React render (avoids Reanimated warning)
+      if (typeof queueMicrotask !== 'undefined') {
+        queueMicrotask(runWorklet);
+      } else {
+        setTimeout(runWorklet, 0);
+      }
     },
-    [type, state, handleOnStart, handleOnActive, handleOnEnd]
+    [type, state, gestureSource, handleOnStart, handleOnActive, handleOnEnd, contextRef]
   );
+
   return gestureHandler;
 };
